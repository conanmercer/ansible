- name: Install Docker
  apt:
    name: "{{ packages }}"
    state: present
  become: true
  vars:
    packages:
      - docker-ce
      - docker-ce-cli
      - docker-compose

- name: Start Docker service
  service:
    name: docker
    state: started
    enabled: true
  become: true
